import 'package:flutter/material.dart';

class AppColors {
  // --- WARNA UTAMA (Brand Color) ---
  static const Color primary = Color(0xFF2962FF); // Biru terang seperti di desain
  static const Color primaryDark = Color(0xFF0039CB);
  static const Color primaryLight = Color(0xFF768FFF);

  // --- WARNA STATUS ---
  static const Color success = Color(0xFF00C853); // Hijau untuk Pemasukan
  static const Color error = Color(0xFFD50000);   // Merah untuk Pengeluaran
  static const Color warning = Color(0xFFFFAB00);

  // --- WARNA BACKGROUND ---
  static const Color bgScaffold = Color(0xFFF5F6FA); // Abu-abu sangat muda (Background App)
  static const Color cardWhite = Colors.white;

  // --- WARNA KHUSUS DESAIN BARU ---
  static const Color greenSoft = Color(0xFFE8F5E9); // Background kartu hijau muda
  static const Color greenText = Color(0xFF2E7D32); // Teks hijau tua

  static const Color blueSoft = Color(0xFFE3F2FD); // Background ikon biru muda
  static const Color redSoft = Color(0xFFFFEBEE);  // Background ikon merah muda

  // Identitas Cabang (Tetap disimpan)
  static const Color branchBst = Color(0xFFD97706);
  static const Color branchAlfa = Color(0xFFDC2626);
  static const Color branchSaufa = Color(0xFF7C3AED);
}